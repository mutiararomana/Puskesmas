<% provide(:title, 'Setting') %>
<h1>Setting</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= form_for(@admin) do |f| %>
      <%= render 'form' %>
    <div class="actions">
        <%= f.submit "Save Change", class: "btn btn-primary" %>
  </div>
      <% end %>
    </div>
</div>
