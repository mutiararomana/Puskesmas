<%= form_for(@poli) do |f| %>
  <% if @poli.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poli.errors.count, "error") %> prohibited this poli from being saved:</h2>

      <ul>
      <% @poli.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama %><br>
    <%= f.text_field :nama %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
