<%= form_for(@jenis_kartu) do |f| %>
  <% if @jenis_kartu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jenis_kartu.errors.count, "error") %> prohibited this jenis_kartu from being saved:</h2>

      <ul>
      <% @jenis_kartu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama %><br>
    <%= f.text_field :nama %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
